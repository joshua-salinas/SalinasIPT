/*
Deployment script for CoffeeShop

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "CoffeeShop"
:setvar DefaultFilePrefix "CoffeeShop"
:setvar DefaultDataPath "C:\Users\REMIE\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\REMIE\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[CoffeeShop].[CoffeeName] is being dropped, data loss could occur.

The column [dbo].[CoffeeShop].[IdItem] is being dropped, data loss could occur.

The column [dbo].[CoffeeShop].[TotalAmount] is being dropped, data loss could occur.

The column [dbo].[CoffeeShop].[ItemId] on table [dbo].[CoffeeShop] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[CoffeeShop])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Starting rebuilding table [dbo].[CoffeeShop]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_CoffeeShop] (
    [ItemId]   INT        NOT NULL,
    [ItemName] NCHAR (10) NULL,
    [Price]    NCHAR (10) NULL,
    [Quantity] NCHAR (10) NULL,
    [Total]    NCHAR (10) NULL,
    PRIMARY KEY CLUSTERED ([ItemId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[CoffeeShop])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_CoffeeShop] ([Price], [Quantity])
        SELECT [Price],
               [Quantity]
        FROM   [dbo].[CoffeeShop];
    END

DROP TABLE [dbo].[CoffeeShop];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_CoffeeShop]', N'CoffeeShop';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Altering Procedure [dbo].[CreateCoffeeShop]...';


GO
ALTER PROCEDURE [dbo].[CreateCoffeeShop]
	@ItemId NVARCHAR (10) = NULL,
    @ItemName NVARCHAR(10) = NULL, 
    @Price INT = NULL, 
    @Quantity INT = NULL,
    @Total INT NULL
    AS
 BEGIN 
    INSERT INTO CoffeeShop (ItemId, ItemName, Quantity, Price, Total)
      Values(@ItemId, @ItemName, @Quantity, @Price, @Total);
End
GO
PRINT N'Altering Procedure [dbo].[GetAllCoffeeShop]...';


GO
ALTER PROCEDURE [dbo].[GetAllCoffeeShop]
	@param1 int = 0,
	@param2 int
AS
	SELECT @param1, @param2
RETURN 0
GO
PRINT N'Altering Procedure [dbo].[UpdateCoffeeShop]...';


GO
ALTER PROCEDURE [dbo].[UpdateCoffeeShop]
	@param1 int = 0,
	@param2 int
AS
	SELECT @param1, @param2
RETURN 0
GO
PRINT N'Altering Procedure [dbo].[DeleteCoffeeShop]...';


GO
ALTER PROCEDURE [dbo].[DeleteCoffeeShop]
	@ItemId NVARCHAR(10) = NULL
AS
BEGIN
	DELETE FROM [dbo].[CoffeeShop] WHERE ItemId = @ItemId
end
GO
/*
Post-Deployment Script Template							
--------------------------------------------------------------------------------------
 This file contains SQL statements that will be appended to the build script.		
 Use SQLCMD syntax to include a file in the post-deployment script.			
 Example:      :r .\myfile.sql								
 Use SQLCMD syntax to reference a variable in the post-deployment script.		
 Example:      :setvar TableName MyTable							
               SELECT * FROM [$(TableName)]					
--------------------------------------------------------------------------------------
*/
GO

GO
PRINT N'Update complete.';


GO
